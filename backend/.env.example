# Backend Configuration

# Network Configuration (devnet, testnet, or mainnet)
NETWORK=devnet

# Aptos Node URL
APTOS_NODE_URL=https://fullnode.devnet.aptoslabs.com/v1

# Server Private Key (for signing transactions)
# This should be your server's Aptos account private key
# Generate with: aptos key generate --output-file server-key
SERVER_PRIVATE_KEY=your_server_private_key_here

# Admin Private Key (for contract administration)
# This is the account that deployed the contract and can update settings
ADMIN_PRIVATE_KEY=your_admin_private_key_here

# PepAsur Contract Address (deployed object address)
PEPASUR_APTOS_CONTRACT_ADDRESS=your_contract_address_here

# Server Port
PORT=3001

# Default Game Settings (in seconds)
DEFAULT_NIGHT_PHASE_DURATION=30
DEFAULT_RESOLUTION_PHASE_DURATION=10
DEFAULT_TASK_PHASE_DURATION=30
DEFAULT_VOTING_PHASE_DURATION=10
DEFAULT_MAX_TASK_COUNT=4

# Faucet Configuration
# The server wallet will need APT balance to fund the faucet
# Users can claim 0.01 APT once every 24 hours
# Make sure SERVER_PRIVATE_KEY account has sufficient balance
